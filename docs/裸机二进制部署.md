> 本页为 [项目 Wiki](https://github.com/hulisang/vmqfox-backend/wiki) 的离线副本（同步于 2026-09-01），在线版以 Wiki 为准。

# 裸机二进制部署

> 适用：不使用 Docker，直接在一台装有 MySQL 的服务器上运行（本仓库开发调试即此模式）。

## 1. 构建二进制

前提：Go 1.26.5+、Node 24 + pnpm 11（版本与 CI 一致，见 `.github/workflows/build-and-release.yml`）。

```bash
# ① 先构建前端（产物写入 internal/http/static/out，由 go:embed 嵌入二进制）
cd web && pnpm install --frozen-lockfile && pnpm build && cd ..

# ② 再编译 Go（顺序不能颠倒，否则根路径没有页面）
go build -trimpath -ldflags="-s -w" -o vmqfox-api ./cmd/vmqfox-api
```

也可以直接从 GitHub Releases 下载预编译的多架构二进制（`vmqfox-backend-<版本>.tar.gz` 内含各平台可执行文件、`schema.sql` 与 `env.example`），跳过构建。

## 2. 配置 .env

```bash
cp env.example .env   # 本机/单机调试
# 或 cp env.production.example .env   # 生产
```

每个变量的中文注释与默认值见仓库 `env.example` / `env.production.example`（与 `internal/config/config.go` 全量核对过）。必填三件套：`VMQ_TOKEN_SECRET`（≥32 字符）、`VMQ_TOKEN_ISSUER`、`VMQ_TOKEN_TTL`，缺失直接启动失败。

## 3. 初始化数据库与管理者（无需导入 SQL）

一条命令即可完成「建表 + 初始化管理员」：`./vmqfox-api -init-admin` 检测到库表不存在时会自动创建全部数据表，再引导设置管理员账号与密码。**不再需要手工导入 `database/schema.sql`**——该文件仅供 DBA 审阅表结构或特殊场景手工建库时使用。

```bash
# 交互式（推荐，密码不回显、二次确认；缺表时自动建表）
./vmqfox-api -init-admin

# 自动化
./vmqfox-api -init-admin -username admin -password 'YourSecurePassword123' -force
# 或经标准输入管道传入密码，避免进程参数泄露
printf '%s\n' 'YourSecurePassword123' | ./vmqfox-api -init-admin -username admin -force
```

只想单独建表、暂不创建管理员时，运行 `./vmqfox-api -init-db`。注意服务进程本身启动时不会自动建表，务必先完成初始化再启动。

从旧 Go-only 版本升级时，必须在对外放流前执行公开令牌迁移（可重复执行，幂等）：

```bash
./vmqfox-api -migrate-public-tokens
```

## 4. 启动与 systemd 常驻

```bash
./vmqfox-api   # 前台运行验证：浏览器打开 http://127.0.0.1:8080
```

`/`（管理台+收银台）、`/assets/*`（带哈希的静态资源）、`/api/*`（接口）三者同源。

```ini
# /etc/systemd/system/vmqfox-api.service
[Unit]
Description=VMQFox Payment Gateway
After=network.target mysql.service

[Service]
Type=simple
WorkingDirectory=/opt/vmqfox
ExecStart=/opt/vmqfox/vmqfox-api
Restart=always
RestartSec=3
# 以 .env 中 VMQ_* 为准；也可在此显式注入环境变量（环境变量优先于 .env）

[Install]
WantedBy=multi-user.target
```

```bash
systemctl daemon-reload && systemctl enable --now vmqfox-api
```

## 5. 验证清单

```bash
curl -s http://127.0.0.1:8080/health   # 期望 healthy
curl -s http://127.0.0.1:8080/ready    # 期望 ready
```

随后登录管理台 → 系统设置填「通讯密钥」→ 按[监控端 App 配置](https://github.com/hulisang/vmqfox-backend/wiki/监控端App配置)绑定挂机手机 → 按[epay 商户插件](https://github.com/hulisang/vmqfox-backend/wiki/epay插件)接入商户系统。

## 6. Windows 下运行

V3.0 发布包内含 `vmqfox-api-windows-amd64.exe`（Intel/AMD 芯片）与 `vmqfox-api-windows-arm64.exe`（骁龙等 ARM 设备），纯 Go 编译，无需安装任何运行时。

1. **解压改名**：Windows 10+ 可直接双击解压 `.tar.gz`（或用 `tar -xzf`）。按 CPU 架构选择对应 exe，重命名为 `vmqfox-api.exe`，放入独立目录（如 `C:\vmqfox`）。
2. **准备数据库**：与 Linux 相同，先装好 MySQL 8+ 并创建空库与业务账号。
3. **配置 .env**：把包内 `env.example` 复制到 exe 同目录并改名 `.env`，按中文注释填写，至少包含数据库必填项、`VMQ_TOKEN_SECRET`、`VMQ_FRONTEND_URL`。
4. **初始化（自动建表，无需导入 SQL）**：在程序目录打开 PowerShell 执行

   ```powershell
   .\vmqfox-api.exe -init-admin
   ```

5. **启动验证**：前台运行 `.\vmqfox-api.exe`，浏览器打开 `http://127.0.0.1:8080` 进入管理台，`/health`、`/ready` 检查同上节。
6. **常驻后台**：Windows 没有 systemd，推荐用 [NSSM](https://nssm.cc/) 注册为服务：`nssm install vmqfox-api C:\vmqfox\vmqfox-api.exe`，并把 Working Directory 设为 `C:\vmqfox`（否则读不到 `.env`）；也可用任务计划程序「用户登录时」触发。
7. **局域网放行**：需要其他设备访问时开放防火墙端口：

   ```powershell
   New-NetFirewallRule -DisplayName vmqfox -Direction Inbound -Action Allow -Protocol TCP -LocalPort 8080
   ```

> [!NOTE]
> `-init-admin` 的交互式密码输入在 PowerShell 下工作正常；无人值守场景用 `.\vmqfox-api.exe -init-admin -username admin -password 'xxx' -force`。收银台跳转要求 `VMQ_FRONTEND_URL` 填买家设备可达的地址（局域网测试填本机内网 IP，不能留 `127.0.0.1`）。
